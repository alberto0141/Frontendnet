@using frontendnet.Models.Validation
@model Categoria

@{
    ViewData["Title"] = "Categorías";
    ViewData["SubTitle"] = "Editar";

    var hasValidModel = Model is not null && Model.CategoriaId.HasValue && Model.CategoriaId.Value > 0;
    var categoriaId = hasValidModel ? Model!.CategoriaId!.Value : 0;
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb small">
        <li class="breadcrumb-item">
            <a class="text-decoration-none"
               title="Regresar al listado"
               asp-action="Index">
                Listado
            </a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">
            @ViewData["SubTitle"]
        </li>
    </ol>
</nav>

<h2 class="text-center mb-3">
    @ViewData["Title"]
    <small class="text-muted fs-5">@ViewData["SubTitle"]</small>
</h2>

@if (!hasValidModel)
{
    <div class="alert alert-warning" role="alert">
        No fue posible cargar la información de la categoría.
    </div>

    <div class="text-center mt-3">
        <a class="btn btn-sm btn-outline-secondary"
           asp-action="Index"
           title="Cancelar">
            Cancelar
        </a>
    </div>
}
else
{
    <form asp-action="Editar"
          asp-route-id="@categoriaId"
          method="post"
          autocomplete="off">
        @Html.AntiForgeryToken()

        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

        <div class="card mt-3">
            <div class="card-body small">
                <div class="form-floating mb-3">
                    <input asp-for="CategoriaId"
                           id="CategoriaId"
                           value="@categoriaId"
                           placeholder="CategoriaId"
                           readonly
                           class="form-control-plaintext"
                           aria-describedby="categoriaIdHelp" />

                    <label for="CategoriaId" class="form-label">
                        @Html.DisplayNameFor(model => model.CategoriaId)
                    </label>

                    <div id="categoriaIdHelp" class="form-text small">
                        Identificador de la categoría actual.
                    </div>
                </div>

                <div class="form-floating mb-3">
                    <input asp-for="Nombre"
                           id="Nombre"
                           type="text"
                           placeholder="Nombre"
                           class="form-control"
                           maxlength="@CategoryValidation.NameMaxLength"
                           minlength="@CategoryValidation.NameMinLength"
                           aria-describedby="nombreHelp" />

                    <label for="Nombre" class="form-label">
                        @Html.DisplayNameFor(model => model.Nombre)
                    </label>

                    <span asp-validation-for="Nombre" class="text-danger"></span>

                    <div id="nombreHelp" class="form-text small">
                        Ingrese un nombre válido para la categoría.
                    </div>
                </div>
            </div>
        </div>

        <div class="text-center mt-3">
            <button id="btnEnviar"
                    type="submit"
                    class="btn btn-sm btn-primary">
                Guardar
            </button>

            <a class="btn btn-sm btn-outline-secondary"
               asp-action="Index"
               title="Cancelar">
                Cancelar
            </a>
        </div>
    </form>
}